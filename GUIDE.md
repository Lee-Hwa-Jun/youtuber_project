# 유튜브 쇼츠 제작 도구 모음 — 사용 가이드

랭킹 쇼츠("Ranking Funniest Dog Reactions to Vacuum Cleaners" 같은 영상)를 만들 때 쓰는 도구들과, 수면 주파수 채널용 소스 생성기, 참고 영상 다운로더의 사용법입니다.
모두 **설치·서버·빌드 없이** 파일 하나 또는 폴더 하나로 동작하고, 데이터는 **내 브라우저 안에만** 저장됩니다. (다운로더만 yt-dlp 를 한 번 설치합니다.)

---

## 한눈에 보기

| 도구 | 하는 일 | 실행 방법 | API 키 |
|---|---|---|---|
| 📈 **경쟁 채널 트래커** | 경쟁 채널 조회수를 주기적으로 찍어 **지금 터지는 영상·제목 패턴·업로드 시간대**를 발견 | `tracker.html` 더블클릭 | 필요 |
| 🔢 **Feed Sorter** | TikTok·Instagram 피드를 **조회수순으로 재정렬**하고 썸네일에 조회수·해시태그를 표시하는 크롬 확장 | 크롬에 폴더 로드 | 불필요 |
| 🎬 **소스 수집 매니저** | 찾은 클립을 **기록·라이선스 관리**하고 5위→1위 **랭킹 배치** | `index.html` 더블클릭 | 선택 |
| 🌌 **소스 생성기** | 수면 주파수 영상용 **무한 루프 영상(webm)** 과 **주파수 음원(wav)** 을 따로 생성 | `source-gen.html` 더블클릭 (Chrome/Edge) | 불필요 |
| ⬇️ **영상 다운로더** | 유튜브·인스타·틱톡 링크를 **자동 인식**해 **원본 화질 다운로드 스크립트(.bat/.sh)** 생성 (yt-dlp 리모컨) | `downloader.html` 더블클릭 + yt-dlp 설치(한 번) | 불필요 |

> 앞의 셋은 **랭킹 쇼츠** 제작 흐름이고, 소스 생성기는 **수면·명상 주파수 채널**용 별도 도구, 영상 다운로더는 **참고용 클립을 원본 화질로 저장**할 때 쓰는 보조 도구입니다.

---

## 추천 작업 흐름

세 도구는 이 순서로 이어 쓰도록 만들어졌습니다.

**1단계 · 무엇이 터지는지 본다 — 트래커**
경쟁 채널을 등록해 두고 하루 1~2번 새로고침합니다. 급상승 탭에서 시간당 조회수가 높은 영상을 보고, 제목 패턴 탭에서 "funniest / worst / reactions" 같은 자주 쓰이는 단어를 확인합니다. 주제가 잡히면 **📋 소스 수집** 버튼으로 제목을 복사합니다.

**2단계 · 그 주제의 클립을 찾는다 — Feed Sorter + 소스 매니저 검색어 탭**
소스 매니저의 검색어 탭에 주제를 넣고 플랫폼 버튼으로 TikTok·Instagram을 엽니다. Feed Sorter가 켜져 있으면 검색 결과가 **조회수순으로 재정렬**되고 썸네일에 조회수가 붙어 있어, 어떤 클립이 반응이 좋은지 스크롤 몇 번으로 파악됩니다. 해시태그 칩을 누르면 연관 태그로 바로 넘어갑니다.

**3단계 · 후보를 기록하고 허락을 받는다 — 소스 매니저**
괜찮은 클립의 URL을 클립 DB에 붙여넣고 웃긴 포인트·점수·원작자 계정을 적습니다. 원작자에게 DM으로 사용 허락을 요청하고 상태를 「DM 보냄」→「허락받음」으로 바꿉니다. 7일 넘게 답이 없으면 대시보드가 알려줍니다.
허락받은 클립은 **영상 다운로더**에 링크를 붙여 원본 화질로 내려받아 편집기에 넣습니다.

**4단계 · 순서를 정한다 — 소스 매니저 랭킹 보드**
허락받은 클립만 후보로 올라옵니다. 5위→1위로 배치하고 클립 길이를 넣어 총 러닝타임을 35~50초에 맞춥니다.

> 💡 저장은 각 도구가 자기 브라우저 저장소에 따로 합니다. 작업이 끝나면 **각 도구의 설정 탭에서 JSON 백업**을 받아 두세요.

---

## 공통 준비물 · YouTube API 키 (한 번만)

트래커에는 필수, 소스 매니저의 유튜브 자동 검색에는 선택입니다. 무료이고 5분 걸립니다.

1. Google Cloud Console(console.cloud.google.com)에 구글 계정으로 접속
2. 상단에서 **새 프로젝트** 만들기 (이름은 아무거나)
3. **API 및 서비스 → 라이브러리** → `YouTube Data API v3` 검색 → **사용 설정**
4. **API 및 서비스 → 사용자 인증 정보 → 사용자 인증 정보 만들기 → API 키**
5. `AIza…`로 시작하는 키를 복사해 각 도구의 **설정 탭**에 붙여넣기

> 🔒 키는 브라우저에만 저장되고 어떤 서버로도 가지 않습니다. JSON 백업에도 일부러 넣지 않습니다.
> 파일(`file://`)로 여는 도구라서, 키에 **HTTP 리퍼러 제한**이 걸려 있으면 403이 납니다. 제한을 "없음"으로 두세요.

---

# 📈 1. 경쟁 채널 트래커

경쟁 채널 10~20개를 등록해 두고 조회수 스냅샷을 쌓아, **어떤 영상이 지금 빠르게 크는지** 찾는 도구입니다.

### 실행
`competitor-tracker/tracker.html` 더블클릭. 설정 탭에 API 키를 넣습니다.

### 처음 5분
1. **채널 탭** — 경쟁 채널 주소(`youtube.com/@이름`), `@이름`, 채널ID(`UC…`) 아무 형식이나 붙여넣고 등록
2. **전체 새로고침** — 각 채널의 최신 쇼츠와 현재 조회수를 저장 (스냅샷 1개)
3. **몇 시간 뒤 다시 새로고침** — 스냅샷이 2개가 되면 **증가 속도**가 계산됩니다
4. **급상승 탭**에서 시간당 조회수가 높은 영상 확인
5. 이후 하루 1~2번만 새로고침하면 됩니다. 자동 폴링은 없습니다.

### 화면별 설명

**🚀 급상승** — 메인 화면. 모든 채널의 쇼츠를 조회수 증가 속도순으로 줄 세웁니다.

| 항목 | 뜻 |
|---|---|
| 시간당 조회수 | 핵심 지표. 클수록 지금 빠르게 크는 중 |
| 추이 | 스냅샷을 이은 미니 그래프. 마우스를 올리면 최초·최근 값 |
| 근거 표시 | "최근 6시간 증가분 기준" 또는 "스냅샷 1개 — 게시 후 평균" |
| 📋 소스 수집 | 제목을 클립보드로 복사 |

필터: 특정 채널만 · 게시 7일 이내만 · 정렬(시간당 / 총 조회수 / 최신)

**🔤 제목 패턴** — 잘 나가는 영상 제목의 단어·2단어 조합 빈도. **형용사(funniest, worst…)** 와 **소재 명사(dog, vacuum…)** 를 나눠 보여줍니다. 단어를 누르면 그 단어가 든 영상만 모아 봅니다.

**📺 채널** — 채널 카드: 구독자·증가분 / 최근 30일 업로드 수 / 평균·중앙값 조회수 / 최고 히트작. **업로드 요일×시간대 히트맵**으로 그 채널이 언제 올리는지 봅니다(밝을수록 많음, 칸에 마우스를 올리면 개수).

**💡 키워드** — 시드 키워드에 a~z를 붙여 유튜브 자동완성을 27번 물어 모읍니다. **API 쿼터를 쓰지 않습니다.** 막히면 검색 링크로 대체됩니다. CSV 내보내기 가능.

**🙋 내 채널** — 내 채널 영상을 모아 **클로드에게 분석시킬 CSV 한 장**을 만드는 탭입니다. 경쟁 채널과는 분리됩니다.
1. 내 채널 주소를 등록하면 최신 200개(쇼츠·롱폼 모두)를 자동 수집. **더보기**로 50개씩 추가
2. (선택) 유튜브 스튜디오 → 분석 → 콘텐츠 표 → **내보내기 → CSV** 파일을 점선 영역에 끌어다 놓으면 노출수·클릭률·시청 지속 시간이 합쳐집니다. 한국어·영어 헤더 모두 인식하고, 못 맞춘 행은 드롭다운으로 직접 고릅니다.
3. 요약 카드 · 조회수 상위 20% 공통점(자주 쓴 단어·평균 길이·최빈 요일/시간대) · 영상 표 확인
4. **분석용 CSV 내보내기** → 아래 **추천 질문 3개** 중 하나를 복사 → 클로드에 CSV 를 첨부하고 붙여넣기

CSV 컬럼: videoId · 제목 · 제목길이 · 이모지포함 · 게시일/요일/시간(KST) · 경과일 · 길이 · 유형 · 조회/좋아요/댓글 · 비율 · 시간당조회수 · 증가율 · (병합 시) 노출수 · 클릭률 · 평균시청 · 조회율 · 구독증감

**⚙️ 설정** — API 키 / 채널당 페이지 수 / 스냅샷 보관 개수 / JSON 백업·가져오기 / 정리.

### 쿼터(할당량)
YouTube API는 하루 **10,000유닛** 무료입니다. 이 도구는 100유닛짜리 검색 API를 **아예 쓰지 않아서** 아주 저렴합니다.

| 작업 | 비용 |
|---|---|
| 채널 1개 새로고침 | 2유닛 |
| 채널 20개 전체 새로고침 | 41유닛 → 하루 약 240번 가능 |
| 내 채널 등록 + 200개 수집 | 9유닛 (이후 새로고침은 2 + 영상 50개당 1) |

화면 맨 아래 바에 오늘 사용량·잔여량·리셋까지 남은 시간이 항상 보입니다. 리셋은 **태평양 시간 자정**(한국 오후 4~5시)입니다.

### 자주 묻는 질문

**Q. 시간당 조회수가 전부 "게시 후 평균"이라고 나와요**
스냅샷이 아직 1개라서입니다. 몇 시간 뒤 한 번 더 새로고침하면 실제 증가 속도로 바뀝니다.

**Q. 롱폼 영상은 안 보여요**
3분 넘는 영상은 트래킹하지 않습니다. 쇼츠 전용 도구입니다.

**Q. "할당량을 모두 사용했습니다"**
오늘 10,000유닛을 다 썼습니다. 태평양 시간 자정에 풀립니다.

**Q. 스튜디오 CSV 가 "미매칭"이 많아요**
스튜디오 표에 **콘텐츠**(영상 ID) 열이 있으면 거의 다 맞습니다. 제목만 있으면 제목을 바꾼 영상은 못 맞출 수 있어요 — 미매칭 목록의 드롭다운에서 직접 고르세요. 내 채널 탭에 아직 수집 안 된 영상은 **더보기**로 먼저 가져와야 합니다.

**Q. `/c/이름` 주소가 안 돼요**
옛 형식 주소는 검색 API 없이는 못 풉니다. 채널 페이지에서 **@핸들**이나 **채널ID**를 복사해 넣어주세요.

---

# 🔢 2. Feed Sorter (크롬 확장)

TikTok·Instagram의 검색 결과·프로필 페이지를 **조회수·좋아요·댓글·날짜순으로 재정렬**하고, 썸네일 위에 조회수와 해시태그를 보여주는 개인용 리서치 도구입니다.

### 설치 (웹스토어 없이 직접)
1. 크롬 주소창에 `chrome://extensions` 입력
2. 오른쪽 위 **개발자 모드** 켜기
3. **압축해제된 확장 프로그램을 로드** → `feed-sorter` 폴더 선택
4. 툴바 퍼즐 아이콘 → Feed Sorter 옆 **압정**으로 고정

> 설치 뒤 이미 열려 있던 TikTok/Instagram 탭은 **F5**로 새로고침해야 붙습니다.
> 코드를 고친 뒤에도 `chrome://extensions`에서 ↻ 새로고침 + 페이지 F5.

### 사용법
확장 아이콘을 누르면 팝업이 열립니다.

| 기능 | 설명 |
|---|---|
| 정렬 | 조회수↓ / 좋아요↓ / 댓글↓ / 최신 / 오래된 / 원래 순서. 사이트별로 기억됩니다 |
| 최소 조회수 필터 | 기준 미달 영상을 흐리게(25%). 마우스를 올리면 다시 보임 |
| 스탯 뱃지 | 썸네일 왼쪽 아래 `👁 1.5M · ❤ 220K · 💬 3.4K` |
| 조회수 테두리 | 100만+ 금색 / 10만+ 초록 |
| 게시일 | 뱃지에 `3mo ago` 추가 |
| 해시태그 칩 | 썸네일 왼쪽 위. **클릭하면 그 태그 검색이 새 탭**으로. 3개 넘으면 `+N` |
| 다운로드 버튼 | 썸네일에 마우스를 올리면 오른쪽 위 ⬇. 다운로드 폴더의 `FeedSorter/`에 저장 |
| CSV 내보내기 | 이번 세션에 수집된 영상 목록 (해시태그 컬럼 포함) |

### 어떻게 데이터를 얻나요
사이트가 **스스로 받아오는 응답을 읽기만** 합니다. 요청을 새로 만들지 않고, 자동 스크롤·대량 수집도 하지 않습니다. 스크롤은 직접 하셔야 하고, 데이터는 사이트가 응답을 줄 때 채워집니다.

> ⚖️ 남의 영상은 **리서치·검토용**으로만 쓰세요. 실제 영상에 넣으려면 원작자 허락이 필요합니다 — 소스 매니저의 라이선스 상태로 관리하세요.

### 자주 묻는 질문

**Q. 뱃지가 안 떠요**
① 페이지를 F5 했는지 ② 피드를 조금 스크롤했는지 확인하세요. 계속 안 뜨면 F12 → Console의 `[Feed Sorter]` 경고가 어디를 고쳐야 하는지 알려줍니다.

**Q. 스크롤하면 화면이 튀어요**
초기 버전의 버그였고 수정되었습니다. 지금은 스크롤이 멈춘 뒤에만 재정렬하고, 보고 있던 카드 위치를 유지하도록 보정합니다. 그래도 튄다면 언제(스크롤 중 / 멈춘 직후) 튀는지 알려주세요.

**Q. 다운로드가 중단돼요**
플랫폼 CDN이 Referer를 요구하는 주소일 수 있습니다. F12 → Console에 원본 주소가 찍히니 새 탭에 붙여넣고 오른쪽 클릭 → "다른 이름으로 비디오 저장".

**Q. 인스타 탐색(/explore/) 페이지가 어색해요**
큰 타일과 작은 타일이 섞인 레이아웃이라 정렬이 어색할 수 있습니다. 검색 결과·프로필처럼 균일한 격자에서 가장 잘 맞습니다.

---

# 🎬 3. 랭킹 쇼츠 소스 수집 매니저

찾은 클립을 기록하고, 원작자 허락 상태를 관리하고, 5위→1위 랭킹을 배치하는 도구입니다.

### 실행
`ranking-shorts-source-manager/index.html` 더블클릭.

### 화면별 설명

**📊 대시보드** — 목표 대비 수집 진행률 / 카테고리별 현황 / 라이선스 확보율 / **DM 보낸 지 7일 넘은 클립 알림**.

**🔎 검색어** — 주제 키워드를 넣으면 변형 검색어를 만들어 줍니다. 각 검색어 옆 버튼으로 YouTube(4분 미만 필터) · Shorts · TikTok · Instagram · Reddit · 이미지 역검색이 새 탭으로 열립니다.

**▶️ YouTube 검색** — (선택) API 키가 있으면 앱 안에서 4분 미만 영상을 검색하고 결과를 바로 클립 DB에 담습니다.

**🎞 클립 DB** — 핵심 화면. URL을 붙여넣으면 플랫폼이 자동 감지되고 유튜브면 썸네일이 붙습니다.

| 입력 항목 | 중요도 |
|---|---|
| URL | 필수 |
| 원작자 계정 | ⭐ DM 보낼 때 필요 |
| 웃긴 포인트 한 줄 | ⭐ 편집 때 큰 도움 |
| 카테고리 | 무서워하는 / 맞서 싸우는 / 반전·의외의 반응 |
| 임팩트 점수 ★1~5 | 랭킹 순서의 기준 |
| 라이선스 상태 | ⭐ 아래 표 |

**라이선스 상태 6단계**

| 상태 | 뜻 | 랭킹 보드 |
|---|---|---|
| ⚪ 미확인 | 아직 연락 안 함 | ✗ |
| ✉️ DM 보냄 | 허락 요청 중. 7일 지나면 대시보드가 알림 | ✗ |
| ✅ 허락받음 | 원작자 승낙 | ✓ |
| 🅭 CC·퍼블릭도메인 | 자유 이용 | ✓ |
| 💳 라이선스 구매 | 유료 구매 | ✓ |
| ⛔ 사용불가 | 거절 또는 불가 | ✗ |

**🏆 랭킹 보드** — 라이선스 확보 클립만 후보로 뜹니다. PC는 드래그, 모바일은 후보 탭 → 슬롯 탭. **✨ 점수순 자동 배치** 후 손보는 방식을 추천합니다.

| 순위 | 권장 |
|---|---|
| 5위 | 무난하지만 확실한 웃음. 짧고 바로 이해되는 클립 |
| 4위 | 리액션 한 단계 더 큰 클립 |
| 3위 | 앞뒤와 다른 카테고리로 텐션 환기 |
| 2위 | 임팩트 최상위권 |
| 1위 | 최고 임팩트 또는 반전 |

각 슬롯에 클립 길이(초)를 넣으면 합산됩니다. **35~50초**가 권장이고 벗어나면 경고합니다.

**⚙️ 설정** — 프로젝트 이름·목표 개수·카테고리 / API 키 / JSON 백업·가져오기 / CSV.

### 자주 묻는 질문

**Q. 랭킹 보드에 후보가 없어요**
라이선스가 「허락받음 / CC·퍼블릭도메인 / 라이선스 구매」인 클립만 올라옵니다. 클립 DB에서 상태를 바꿔주세요.

**Q. 내보낸 파일 이름이 영어예요**
파일로 여는 앱은 크롬이 한글 파일명을 버리고 확장자 없는 `download`로 저장해 버려서, 이름만 영어로 만듭니다. 내용의 한글은 그대로입니다.

**Q. 데이터가 사라졌어요**
브라우저 데이터를 지웠거나 시크릿 창에서 썼을 가능성이 큽니다. 일반 창에서 쓰고 JSON 백업을 주기적으로 받아 두세요.

---

# 🌌 4. 소스 생성기 (수면 주파수 영상용)

몽환적인 **무한 루프 영상**과 **주파수 음원**을 각각 만들어 내려받는 도구입니다.
최종 합치기는 편집기에서 합니다. 랭킹 쇼츠 흐름과는 별개입니다.

### 실행
`sleep-source-generator/source-gen.html` 더블클릭. **Chrome 또는 Edge** 에서 여세요 (영상 녹화 기능 때문).

### 🌌 비주얼 생성기 탭
1. 패턴(네뷸라 · 별밭 · 숨쉬는 원 · 오로라 · 유체 연기 · 반딧불) 과 팔레트(남색 · 보라 · 청록 · 앰버 · 모노) 선택
2. 속도 · 밀도 · 글로우 · 배경 밝기 슬라이더로 분위기 조절
3. 해상도(FHD / 4K / 세로) · fps · 비트레이트 · 길이 선택
4. **녹화 시작** → 실시간 녹화 → 끝나면 webm 저장 대화상자

- 녹화 길이는 애니메이션 **주기의 정수배로 자동 스냅**됩니다 (화면에 "요청 3분 → 실제 189초 = 63초 × 3회" 처럼 표시).
  그래서 편집기에서 같은 파일을 여러 번 이어붙여도 이음새가 없습니다.
- **녹화 중 탭을 벗어나면** 프레임이 끊깁니다. 경고가 뜨면 다시 녹화하세요.
- 4K 는 30fps 를 권합니다. **정지 이미지 PNG** 로 썸네일도 뽑을 수 있습니다.

### 🎧 주파수 음원 생성기 탭
1. Hz 입력 또는 자주 쓰는 값 칩(174 · 432 · 528 · 852 …)으로 항목 추가
2. 항목마다 모드 선택

| 모드 | 설명 |
|---|---|
| 퓨어 톤 | 단일 사인파 |
| 바이노럴 비트 | 왼쪽 캐리어 / 오른쪽 캐리어+비트 (예: 200Hz / 204Hz). 이어폰 필수 |
| 톤 + 노이즈 | 사인파에 핑크 또는 브라운 노이즈를 비율대로 섞음 |

3. 길이(1~60분) · 볼륨(dBFS, 기본 −12) · 페이드인/아웃(기본 3초) 설정
4. **미리듣기**로 확인 → **WAV 생성** (44.1kHz · 16bit · 스테레오)

- 파일명에 설정이 들어갑니다: `tone_432Hz_10min.wav`, `binaural_200Hz_4Hz_30min.wav`
- 사인파는 **정확히 정수 사이클**로 끝나서 파일을 이어붙여도 클릭이 없습니다.
- 30분 이상은 메모리를 많이 씁니다. 실패하면 절반으로 나눠 만들고 이어붙이세요 (클릭 없음).

### 편집기에서 합치기
음원을 먼저 깔고 → 영상 클립을 원본 길이 그대로 복사해 끝까지 이어붙이기 → 마지막만 음원 길이에 맞춰 자르고 페이드아웃.
webm 을 못 읽는 편집기는 HandBrake 로 mp4 변환.

### 자주 묻는 질문

**Q. 녹화가 안 돼요**
Safari 등은 캔버스 녹화를 지원하지 않습니다. Chrome/Edge 에서 여세요.

**Q. 이어붙였는데 이음새가 보여요**
녹화 길이가 스냅된 "실제 N초" 인지 확인하고, 편집기에서 클립을 자르지 말고 원본 길이 그대로 붙이세요.

**Q. 미리듣기 소리가 안 나요**
브라우저 자동재생 정책입니다. 페이지를 한 번 클릭한 뒤 ▶ 를 다시 누르세요.

---

# ⬇️ 5. 영상 다운로더 (yt-dlp 리모컨)

유튜브 · 인스타그램 · 틱톡 링크를 붙이면 **플랫폼을 자동 인식**하고 **원본 화질로 저장하는 스크립트**를 만들어 주는 도구입니다.

> HTML 파일 하나가 스스로 영상을 내려받을 수는 없습니다(브라우저 보안 CORS, 유튜브 고화질의 영상·오디오 분리). 실제 다운로드는 오픈소스 표준 도구 **yt-dlp** 가 하고, 이 페이지는 링크를 인식해 **더블클릭 한 번으로 실행되는 스크립트** 를 만드는 "리모컨"입니다.

### 실행
`video-downloader/downloader.html` 더블클릭.

### 처음 한 번 · yt-dlp 설치
페이지 1번 칸에 OS 별 명령이 복사 버튼과 함께 나옵니다.

- **Windows**: `Win + X` → 터미널 → `winget install yt-dlp.yt-dlp` → `winget install Gyan.FFmpeg`
- **Mac**: 터미널 → `brew install yt-dlp ffmpeg`
- `yt-dlp --version` 에서 버전이 나오면 성공. **터미널을 새로 열어야** 인식됩니다.

ffmpeg 는 유튜브 고화질의 분리된 영상·오디오를 mp4 하나로 합치는 데 필요합니다.

### 사용법
1. 우측 상단에서 **Windows / Mac** 선택
2. **2번 칸에 링크 붙이기** — 여러 개면 줄바꿈. 즉시 플랫폼 배지(▶️ YouTube · 📸 Instagram · 🎵 TikTok)와 정리된 주소가 나오고, 재생목록·채널·프로필 링크는 이유와 함께 거부됩니다
3. **옵션** — 화질(원본 / 4K / 1080p / 720p / 오디오만), 로그인 쿠키 브라우저, 저장 폴더, 파일명 규칙, 자막·썸네일·메타데이터
4. **실행**
   - Windows: `⬇ 다운로드 스크립트 받기 (.bat)` → 받은 파일 **더블클릭**. "PC 보호" 창은 **추가 정보 → 실행**
   - Mac: `.sh` 를 받아 터미널에 `sh ` 입력 후 파일을 드래그 → Enter. 또는 `📋 명령 복사` → 터미널 붙여넣기

인식하는 링크: 유튜브 watch / youtu.be / Shorts / live, 인스타그램 Reel / 게시물(/p/), 틱톡 영상 / vm·vt 단축링크 / 사진 게시물(경고 표시).

### 알아두기
- **인스타그램은 로그인 쿠키가 필수**입니다. 인스타에 로그인해 둔 브라우저를 옵션에서 고르세요. 안 고르면 경고가 뜨고 "login required" 로 실패합니다.
- 스크립트는 기본으로 실행 전 `yt-dlp -U` 자동 업데이트를 합니다. 사이트가 바뀌어 깨졌을 때 대부분 이걸로 해결됩니다.
- `--no-playlist` 가 들어가 있어 재생목록 링크가 섞여도 영상 하나만 받습니다(실수로 수백 개 받는 일 방지).
- 최근 목록에 받은 링크가 기록되고(최대 200개) CSV 로 내보낼 수 있습니다.
- 남의 영상은 **리서치·검토용**으로만. 실제 영상에 쓰려면 소스 수집 매니저에서 허락 상태를 관리하세요.

### 자주 묻는 것
**Q. "yt-dlp 은(는) 내부 또는 외부 명령… 이 아닙니다" / "command not found"**
설치가 안 됐거나 터미널을 다시 열지 않았습니다. 설치 후 새 터미널(또는 .bat)로 다시 실행하세요.

**Q. 영상과 소리가 따로 저장됐어요 / "ffmpeg not found"**
ffmpeg 가 없어서 합치지 못했습니다. 설치 명령을 실행하세요.

**Q. "Sign in to confirm you're not a bot" / 403**
옵션에서 로그인 쿠키 브라우저를 고르세요. 그 브라우저에 해당 사이트가 로그인되어 있어야 합니다.

**Q. "Unsupported URL" / "Unable to extract"**
사이트가 바뀐 것입니다. `yt-dlp -U` 로 업데이트하세요.

---

## 백업 원칙 (랭킹 쇼츠 도구 세 개 공통)

> 소스 생성기는 저장하는 데이터가 없습니다(설정은 새로고침하면 초기화). 만든 webm/wav 파일만 잘 보관하세요.
> 영상 다운로더는 링크 목록·옵션만 저장하며, 내려받은 영상 파일은 내 컴퓨터 폴더에 있습니다.

- 데이터는 **각 도구별로, 그 브라우저에만** 저장됩니다. 크롬에서 만든 것은 사파리에 없습니다.
- 브라우저 기록/데이터를 지우면 함께 사라집니다.
- 작업이 끝나면 **설정 탭 → JSON 내보내기**. 다른 컴퓨터로 옮길 때는 그 파일을 **가져오기**.
- API 키는 백업에 들어가지 않으니 새 기기에서 다시 입력합니다.

## 문제가 생기면

1. **F5**로 새로고침 (확장은 `chrome://extensions`에서 ↻도 함께)
2. **F12 → Console**에서 도구 이름이 붙은 경고를 봅니다. 원인과 고칠 위치를 한국어로 알려주도록 만들어 두었습니다.
3. 그래도 안 되면 각 도구 폴더의 `README.md` 끝부분 **유지보수** 절에 사이트 구조 변경 시 고칠 상수 위치가 정리되어 있습니다.
